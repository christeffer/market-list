/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ItemList from './src/screens/ItemList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ItemList);
